{
  "name": "cadence-tab",
  "version": "1.0.0",
  "private": true,
  "description": "Your developer dashboard — Cadence time tracking, GitHub tasks, bookmarks, and quick links in every new tab.",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "sharp": "^0.33.0"
  }
}
